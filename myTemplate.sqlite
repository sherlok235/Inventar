
 create table items(id integer primary key , name text,descriere text,cantitate integer,i_path text);
 
 SELECT * FROM items;
 
 //add new data 
 INSERT INTO items VALUES(?,?,?,?,?);
 
 //update data
 UPDATE items SET name = ?, descriere = ?,cantitate = ?,i_path = ? WHERE id = ?;
 
